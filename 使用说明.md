# 万能格式转换器（便携版）

> **U 盘便携说明**：本文件夹自带精简 Java 运行时（runtime/），拷到 U 盘后
> 在任何 Windows x64 电脑上双击 `万能转换\万能转换.exe`，浏览器自动打开转换页面。

- 程序会出现在**系统托盘**（右下角闪电图标）：右键可重新打开页面或退出
- 转换好的文件在 24 小时内可随时下载，之后自动清理，**无需安装 Java**。
> 手机访问网页版：与电脑连同一 Wi-Fi，用浏览器打开服务启动时显示的局域网地址。

一个纯 Java 的本地文件转换工具：**桌面版**（Swing 暗色插画界面）+ **网页版**（现代 SaaS 风格，支持手机）。全部本机处理，文件不外传。

## 网页版（推荐体验）

```
启动转换器.bat        → 网页版（浏览器自动打开 http://127.0.0.1:8080/）
桌面版.bat            → 桌面窗口版（批量转换）
```

或命令行：`java -jar FileConverter.jar --web --port 8080`

- 浏览器里拖入文件 → 选目标格式 → 开始转换 → 点「下载文件」，转换完成即可直接下载
- 手机/平板：与电脑连同一 Wi-Fi，访问服务启动时打印的局域网地址（如 http://192.168.x.x:8080）
- 下载接口：`GET /api/download/<任务ID>`，流式输出，中文文件名正确编码
- 转换结果保留 24 小时自动清理；任务 ID 随机生成，外部无法访问任意路径
- 不带 `--web` 参数启动则仍是原来的桌面版，两个版本互不影响

### 网页版 API（供程序调用）

| 接口 | 说明 |
|------|------|
| `POST /api/upload?name=文件名` | 请求体为原始文件字节，返回 `{fileId, fileName, size, targets[], ffmpeg}` |
| `POST /api/convert` | 请求体 `{"fileId":"...","target":"mp3"}`，提交转换 |
| `GET /api/status?id=...` | 返回 `{state: READY/RUNNING/DONE/FAILED, progress, downloadUrl, error}` |
| `GET /api/download/<id>` | 流式下载转换结果，Content-Disposition 支持中文名 |

## 桌面版

双击 `启动转换器.bat`，或命令行执行 `java -jar FileConverter.jar`：

## 支持的转换

| 类别 | 输入格式 | 可转换为 |
|------|----------|----------|
| 视频 | mp4 / mkv / avi / mov / webm / flv / wmv / ts / mpg / 3gp / **m3u8**（含网络链接） | mp4 / mkv / avi / mov / webm / gif，或直接提取音频 mp3 / aac / wav / flac / m4a / ogg |
| 音频 | mp3 / wav / aac / flac / m4a / ogg / wma / opus | mp3 / wav / aac / flac / m4a / ogg / wma |
| **加密音乐** | **ncm**（网易云）/ **kgm、kgma、vpr**（酷狗） | 原格式（解出无损 FLAC / MP3）或转 mp3 / flac / wav；自动按「歌手 - 歌名」命名 |
| 图片 | png / jpg / webp / bmp / gif / tiff / ico | jpg / png / webp / bmp / pdf（多张可合并为一个 PDF） |
| PDF | pdf | docx（Word）/ txt / 按页导出 png、jpg |
| Word | docx | pdf（检测到 LibreOffice 时高保真导出，否则内置简化排版）/ txt |
| 文本 | txt / md | pdf（自动嵌入中文字体）/ docx |
| 表格 | xlsx / xls | csv |
| CSV | csv | xlsx |

## 使用方法

### 图形界面（推荐）

双击 `桌面版.bat`，或命令行执行 `java -jar FileConverter.jar --gui`：

1. 点 **添加文件**（或直接把文件拖进列表；网络 m3u8 用 **添加链接**）
2. 在 **转换为** 下拉框选目标格式（会根据文件类型自动过滤）
3. 需要时勾选 **多张图片合并为一个 PDF**、选择 **音频码率**
4. **输出目录** 留空 = 与源文件相同目录
5. 点 **开始转换**，日志区实时显示进度，可随时 **取消**

### 命令行

```bat
java -jar FileConverter.jar 视频.mp4 --to mp3
java -jar FileConverter.jar playlist.m3u8 --to mp4 -o D:\输出
java -jar FileConverter.jar a.png b.png c.png --to pdf --merge-pdf
java -jar FileConverter.jar 文档.pdf --to docx
java -jar FileConverter.jar 表格.xlsx --to csv
```

选项：`--to/-t 目标格式`（必填）、`-o/--out 目录`、`--bitrate 128k|192k|320k`、`--merge-pdf`。

## 依赖说明

- **FFmpeg**（音视频/图片转换引擎）：已自动检测系统 PATH；检测不到时点界面右上角
  “FFmpeg 未检测到（点击设置）”手动选择 ffmpeg.exe 即可，路径会记住。
  没有 FFmpeg 时仍可使用 PDF / Word / Excel / 文本转换。
- 其余全部随 `lib/` 目录分发（FlatLaf 界面、Apache PDFBox、Apache POI），无需安装。

## 技术架构

```
src/converter/
├── Main.java              入口（无参=GUI，带参=CLI）
├── cli/CliRunner.java     命令行解析
├── core/
│   ├── Engine.java        任务调度：类别识别、目标格式交集、逐个执行
│   ├── MediaConverter.java FFmpeg 进程封装（进度解析、m3u8 无损→重编码回退、取消）
│   ├── PdfService.java    PDFBox：图片⇄PDF、PDF⇄文本、PDF 转图片、中文字体加载
│   ├── OfficeService.java POI：PDF→Word、Word→PDF、Excel⇄CSV
│   ├── FileKind.java      文件类别与目标格式矩阵
│   └── FfmpegLocator.java FFmpeg 定位（配置→PATH→默认目录）
└── ui/ConverterFrame.java Swing 界面（FlatLaf 暗色主题）
```

## 从源码构建

```
build.bat        （或手动: 见脚本内容，javac + jar 两步）
```

## 常见问题

- **PDF 转 Word 后排版变了？** PDF 是“打印结果”格式，转 Word 是按文本/图片重建，
  复杂排版（多栏、表格线）会从简，文字内容完整。
- **扫描版 PDF 转 Word 没有文字？** 扫描件里只有图片没有文本，会提示“可能为扫描件”，
  如需文字需要先做 OCR（本工具不含）。
- **m3u8 在哪里？** 本地 .m3u8 文件直接添加；网络直播/在线视频点“添加链接”粘贴地址。
- **转换 mp4 用什么参数？** H.264 (CRF 23, veryfast) + AAC，兼容性最好；音频默认 192k。

## 音乐解锁说明

- **支持**：网易云 `.ncm`；酷狗 `.kgm` / `.kgma` / `.vpr`（三代加密方案自动识别）
- **选择「原格式」**：直接解出文件内的原始音频——VIP 下载的高品质歌曲即为**无损 FLAC**（或 MP3），不做任何有损转码
- **选择 mp3 / flac / wav**：解锁后经 FFmpeg 转码，方便在不支持 FLAC 的设备上播放
- 输出文件自动按歌曲元数据命名为「歌手 - 歌名.格式」，来源是文件内嵌的元数据
- 算法来自公开的开源项目（ncmdump / unlock-music / KgmWasm），仅用于处理你本人合法下载的歌曲
- **暂不支持**：酷狗 `.kgg`（需要客户端密钥库）、QQ音乐 `.mflac/.mgg`（需要逐曲密钥）
